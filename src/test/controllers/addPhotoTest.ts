import chai, {expect} from "chai";

describe("addPhoto", function () {
	it("I can add a photo with an url and a description", function () {});
	it("on success send status 200 and photo data", function () {});
	it("on error send erro message and status 500", function () {});
	it("if request date is incomplete send an error message and status 400", function () {});
});
